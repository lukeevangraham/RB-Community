<div class="bg_selection">
{{#if blogpost.multipleEntries}}
<div class="container">
  <div class="row pb-2">
    <div class="col">
      <div class="section_title_container text-center">
        <div class="section_title">{{this.request}} news</div>
        <div class="section_subtitle">Be part of a community of people
          experiencing God together.</div>
      </div>
    </div>
  </div>

{{/if}}

<div class="row news_row">

  {{#each blogpost}}
    {{#unless fields.featureOnMinistryPage}}

      <!-- News Post -->
      <div class="col-xl-4 col-lg-6 news_post_col">
        <div class="news_post">
          <div class="news_image"><a
              href="./blog-{{changeSpacesToDashes this.fields.title}}"
            ><img
                src="{{this.fields.image.fields.file.url}}"
                alt=""
              /></a></div>
          <div class="news_post_content">
            <div class="news_post_title">
              <a
                href="./blog-{{changeSpacesToDashes this.fields.title}}"
              >{{this.fields.title}}</a>
            </div>
            <div class="news_post_meta">
              <ul>
                <li><i class="fa fa-user" aria-hidden="true"></i><a
                    href="#"
                  >{{#each fields.author}}{{this}}{{/each}}</a></li>
                <li><i class="fa fa-calendar-o" aria-hidden="true"></i><a
                    href="#"
                  >{{this.fields.formattedDate}}</a></li>
                {{! <li><i class="fa fa-comment" aria-hidden="true"></i><a href="#">15</a></li> }}
              </ul>
            </div>
            <div class="news_post_text">
              {{#if fields.excerpt}}
                <p>{{this.fields.excerpt}}...</p>
              {{/if}}
            </div>
          </div>
        </div>
      </div>

    {{/unless}}
  {{/each}}
</div>
</div>
</div>